name: Keep Streamlit App Awake

on:
  schedule:
    - cron: "17 */6 * * *"
  workflow_dispatch: {}

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Streamlit app
        run: |
          curl -sSf -o /dev/null -w "HTTP %{http_code}\n" "https://july-4-trip-planner-n7rhe3bsyzkeacjbmrytwf.streamlit.app/"
